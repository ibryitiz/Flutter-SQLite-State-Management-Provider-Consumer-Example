class Urun {
  int urunId; // Ürün ID
  String urunAd; // Ürün adı
  double urunFiyat; // Ürün fiyatı
  String urunResim; // Ürün resmi URL'si// Üye ID

  Urun({
    required this.urunId,
    required this.urunAd,
    required this.urunFiyat,
    required this.urunResim,
  });
}
